{
  "name": "shopsite-stripe-api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:put": "wrangler secret put STRIPE_SECRET_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
